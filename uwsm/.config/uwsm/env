export XCURSOR_SIZE=20

export GBM_BACKEND=nvidia-drm
export __GLX_VENDOR_LIBRARY_NAME=nvidia
export LIBVA_DRIVER_NAME=nvidia
export NVD_BACKEND=direct
export __GL_GSYNC_ALLOWED=1
export __GL_VRR_ALLOWED=0

export QT_AUTO_SCREEN_SCALE_FACTOR=1
export QT_QPA_PLATFORM=wayland;xcb
export QT_QPA_PLATFORMTHEME=qt6ct
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1

export XDG_MENU_PREFIX=arch- kbuildsycoca6

# Enable native Wayland support (instead of xwayland) for most Electron apps
# I only use VS Code and Discord and they both work well without this.
# spotify-launcher is also working fine.
# In fact VS Code will have issue on dragging with this enabled.
# export ELECTRON_OZONE_PLATFORM_HINT=auto
